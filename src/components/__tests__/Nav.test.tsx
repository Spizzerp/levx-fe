import { describe, it } from 'vitest'

describe('Nav', () => {
  it.todo('renders the WalletButton in place of the old hardcoded "7K4D···9XQ2" pill (FOUND-13)')
  it.todo('does NOT render any literal "7K4D" string (regression check on the replaced fake button)')
  it.todo('retains all pre-Phase-2 nav items (logo, route links)')
})
