import { describe, it } from 'vitest'

describe('MarketStateBadge', () => {
  it.todo('renders Pending state with "Wagering opens" prose')
  it.todo('renders Active state with "Wager open until" prose')
  it.todo('renders Sampling state with "Final wagers; checkpoint scoring underway" prose')
  it.todo('renders Settling state with "Final score being computed" prose')
  it.todo('renders Maturing state with "Review window; claims open in {countdown}" prose')
  it.todo('renders Settled state with "Claim available" prose')
  it.todo('renders Void state with "Market cancelled" prose')
  it.todo('uses StatusDot with the lifecycle-mapped dot color')
})
