import { describe, it } from 'vitest'

describe('useYAxisFreeze', () => {
  it.todo('returns live domain when not frozen')
  it.todo('returns frozen domain while freeze() is active')
  it.todo('resumes live domain ~300ms after thaw() call')
  it.todo('does not thaw early if thaw() fires multiple times in rapid succession')
})
