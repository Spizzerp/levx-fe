import { describe, it } from 'vitest'

describe('DrawingGrid', () => {
  it.todo('renders one vertical checkpoint column line per checkpoint when in draw mode')
  it.todo('renders horizontal price grid lines when in draw mode')
  it.todo('renders nothing when not in draw mode')
  it.todo('checkpoint column X positions map to buildCheckpointXs(market) domain times via xScale')
})
