import { describe, it } from 'vitest'

describe('lib modules smoke test', () => {
  it.todo('src/lib/pyth exports client, hooks, feedIds, types')
  it.todo('src/lib/drawing exports crossingDetector, drawingStore, geometry, hooks, types')
})
