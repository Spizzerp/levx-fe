import { describe, it } from 'vitest'

describe('buildTransaction', () => {
  it.todo('returns the provided instructions array unchanged in Phase 2 (shell stub) (FOUND-12)')
  it.todo(
    'accepts optional computeUnitLimit and priorityFeeMicroLamports fields without throwing (FOUND-12)',
  )
  it.todo('exported as an async function returning TransactionInstruction[] (FOUND-12)')
})
