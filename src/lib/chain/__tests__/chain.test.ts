import { describe, it } from 'vitest'

describe('src/lib/chain public API', () => {
  it.todo('re-exports useMarkets with the same signature as src/lib/api/hooks (MARKET-06)')
  it.todo('re-exports useMarket with the same signature as src/lib/api/hooks (MARKET-06)')
  it.todo('re-exports useUserPosition with the same signature as src/lib/api/hooks (MARKET-06)')
  it.todo('useMarkets in Phase 2 delegates to the mock implementation and returns mock data shape')
  it.todo('useMarket in Phase 2 delegates to the mock implementation')
  it.todo('exports AnchorProgramProvider and useAnchorProgram from index')
})
