import { describe, it } from 'vitest'

describe('env.config', () => {
  it.todo('throws when APP_HERMES_URL is missing')
  it.todo('throws when APP_RPC_URL is missing')
  it.todo('throws when APP_NETWORK is missing')
  it.todo('returns typed env object when all required values present')
})
