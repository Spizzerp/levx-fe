import { describe, it } from 'vitest'

describe('MarketPage', () => {
  it.todo('opens a Pyth SSE feed on mount for the current market pair')
  it.todo('closes the Pyth SSE feed on unmount')
  it.todo('live price header updates from pythStore ticks')
  it.todo('clicking an AI path in the rail updates selectedPathId')
  it.todo('shows "Drawing requires desktop" notice below md breakpoint (matchMedia mock)')
  it.todo('hides the "+ Draw Custom Path" button below md breakpoint')
  it.todo('wires the draw-mode toggle to drawingStore.enterDrawMode')
})
