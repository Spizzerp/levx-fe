import { describe, it } from 'vitest'

describe('mockFixtures aiPaths', () => {
  it.todo('buildAiPathFixture returns a PredictionPath[] of length 5')
  it.todo('each fixture path has values at every checkpoint X')
  it.todo('fixture paths span all tones (ultra-bull through ultra-bear)')
})
